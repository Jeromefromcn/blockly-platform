# Development Log - 2026-03-25

## Flyway Database Migration Setup

### Completed Tasks
1. Added Flyway dependency to `backend/pom.xml`
   - Version: 10.4.1
   - Artifact: flyway-mysql
   
2. Removed Spring SQL init configuration from `backend/src/main/resources/application.yml`
   - Removed `spring.sql.init` section
   - Flyway now handles all database migrations
   
3. Created Flyway migrations directory structure
   - Location: `backend/src/main/resources/db/migration/`
   
4. Created migration files:
   - **V1__Initial_schema.sql**: Contains all original CREATE TABLE statements
     - Includes all 8 tables: exercises, exercise_versions, likes, submissions, grades, users, role_permissions, categories
     - Contains INSERT IGNORE for default categories
   
   - **V2__Add_exercise_categories_and_difficulty.sql**: Future-proofing migration
     - Uses ALTER TABLE IF NOT EXISTS for idempotency
     - Adds category and difficulty columns (already in V1)
     - Safe for both fresh and existing databases

### Benefits
- Versioned migrations tracked in `flyway_schema_history` table
- Each migration runs exactly once
- Works on both fresh and existing databases
- Industry standard approach for schema management
- Clear audit trail of all schema changes

### Next Steps for Deployment
1. Rebuild Docker container: `docker compose up -d --build backend`
2. Verify Flyway execution: `docker compose logs backend | grep -i flyway`
3. Check database: `docker exec blockly_db mysql -ublockly -pblockly123 blocklydb -e "SELECT * FROM flyway_schema_history;"`

### Files Changed
- `backend/pom.xml` - Added Flyway dependency
- `backend/src/main/resources/application.yml` - Removed spring.sql.init
- `backend/src/main/resources/db/migration/V1__Initial_schema.sql` - Created
- `backend/src/main/resources/db/migration/V2__Add_exercise_categories_and_difficulty.sql` - Created

### Git Commit
- Commit: d5a88d3
- Message: "infra: add Flyway for database migration versioning"
- Branch: develop

---

## Block Palette Restrictions and Hints System (TODO #6, #7)

### Completed Tasks

1. Created Flyway migration V2
   - File: `backend/src/main/resources/db/migration/V2__Add_hints_to_exercise_versions.sql`
   - SQL: `ALTER TABLE exercise_versions ADD COLUMN hints TEXT DEFAULT NULL;`

2. Backend changes
   - `ExerciseVersion.java`: Added `hints` field mapped to `hints` column
   - `ExerciseCreateDto.java`: Added `hints` field (null = no hints)
   - `ExerciseService.java`: `saveVersion()` persists `hints`; `toVersionMap()` returns `hints` in API response
   - Note: `allowedBlocks` was already fully implemented in backend; no changes needed there

3. Frontend: AdminEditor.jsx
   - Replaced `ALL_CATEGORIES` (category names) with `BLOCKLY_BLOCKS` constant (29 individual block types with labels)
   - Replaced `selectedCategories` state with `allowAllBlocks` (bool) + `selectedBlocks` (array of block type strings)
   - Added `hints` state (array of strings)
   - Block Restrictions UI: "Allow all blocks" checkbox toggle; when unchecked, shows grid of individual block checkboxes
   - Hints UI: list of text inputs with Remove buttons; "+ Add Hint" button
   - Load logic: parses `allowedBlocks` JSON into `selectedBlocks`, sets `allowAllBlocks = false` when present; parses `hints` JSON
   - Save logic: sends `allowedBlocks` (null or JSON stringified block types array) and `hints` (null or JSON stringified hints array)

4. Frontend: BlocklyWorkspace.jsx
   - Added `allowedBlocks` prop (array of block type strings)
   - Added `buildFilteredToolbox()` helper: filters toolbox contents at block-type level; removes empty categories; keeps custom categories (Variables, Functions) always
   - `allowedBlocks` takes precedence over `allowedCategories` (backward compat)

5. Frontend: Workspace.jsx
   - Added `parsedHints` and `hintsRevealed` state
   - On exercise load: parses `exercise.version.hints` JSON into `parsedHints`
   - Changed `allowedCategories` prop to `allowedBlocks` in BlocklyWorkspace usage
   - Added hints reveal panel below workspace/output: "Get Hint (n/total)" button; revealed hints shown as amber cards

### Tests
- All 45 backend tests pass: `Tests run: 45, Failures: 0, Errors: 0, Skipped: 0`

### Files Changed
- `backend/src/main/resources/db/migration/V2__Add_hints_to_exercise_versions.sql` — Created
- `backend/src/main/java/com/blocklyplatform/entity/ExerciseVersion.java` — Added `hints` field
- `backend/src/main/java/com/blocklyplatform/dto/ExerciseCreateDto.java` — Added `hints` field
- `backend/src/main/java/com/blocklyplatform/service/ExerciseService.java` — Persist and return `hints`
- `frontend/src/pages/AdminEditor.jsx` — Block restrictions UI (individual blocks) + Hints UI
- `frontend/src/pages/Workspace.jsx` — Hints panel + use `allowedBlocks` prop
- `frontend/src/components/BlocklyWorkspace.jsx` — `allowedBlocks` prop + `buildFilteredToolbox()`
- `RUNBOOK.md` — Documented both features
