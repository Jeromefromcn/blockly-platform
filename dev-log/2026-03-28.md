# Development Log - 2026-03-28

## Fix: Blockly Module Import Error in Grading Panel

### Problem
`executeCode()` used `window.Blockly` which does not exist — Blockly is bundled as an ES module, not a global.

### Fix
- Added `import * as Blockly from 'blockly'` and `import { javascriptGenerator } from 'blockly/javascript'` to Admin.jsx
- `executeCode()` now uses the imported `Blockly` and `javascriptGenerator` directly

### Files Changed
- `frontend/src/pages/Admin.jsx` — Added Blockly ES module imports; updated `executeCode()`

### Git Commit
- Commit: 06eb125
- Branch: develop

---

## Feature: Submissions Tab — Responsive Layout + Keyword Search Filter

### Changes
1. **Responsive panels**: Left panel now uses `flex: '0 0 clamp(260px, 35%, 420px)'` instead of a fixed width; right panel uses `flex: 1` to fill the remaining space.
2. **Keyword search**: Exercise dropdown filter replaced with a free-text input; filters submissions by `exerciseTitle` substring (case-insensitive).
3. **Filter bar moved above panels**: Filter input, status toggle buttons, and submission count are now a full-width row above the two-panel grid, not inside the left panel header.

### Files Changed
- `frontend/src/pages/Admin.jsx` — Submissions tab layout restructured; `applyFilters()` updated; `exerciseOptions` removed

### Git Commit
- Branch: develop

---

## Fix: Submissions Left Panel Still Fixed Width

### Problem
Left panel used `flex: 0 0 clamp(260px, 35%, 420px)` with no grow/shrink; right panel had `display: none` when no submission selected — so left panel couldn't fill available space and appeared locked.

### Fix
- Left panel: `flex: 0 0 320px` (clean fixed width, always visible)
- Right panel: `flex: 1`, always rendered (shows placeholder when nothing selected)
- Removed `showMobilePanel` hide/show logic from both panels

### Files Changed
- `frontend/src/pages/Admin.jsx`

### Git Commit
- Branch: develop

---

## Fix: Back Button Hidden in Grading Panel

### Problem
The "Back to list" button had `display: none` inline style — it was a leftover mobile-only stub that was never wired to a CSS class.

### Fix
- Changed to `display: inline-flex`; button is now always visible
- Clicking also clears `selectedSubmission` and `gradingState` so the right panel returns to the placeholder state

### Files Changed
- `frontend/src/pages/Admin.jsx` — Back button style and onClick updated

### Git Commit
- Commit: 06eb125
- Branch: develop
